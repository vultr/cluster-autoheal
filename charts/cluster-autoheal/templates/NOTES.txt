cluster-autoheal has been installed.

Image:

  {{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}

If using the Vultr provider, set VULTR_API_KEY with either:

  --set vultr.apiKey=...

or create a secret and install with:

  --set vultr.existingSecret=<secret-name>
