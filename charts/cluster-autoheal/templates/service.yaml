apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-autoheal.fullname" . }}
  labels:
    {{- include "cluster-autoheal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-autoheal.selectorLabels" . | nindent 4 }}
